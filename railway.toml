[build]
builder = "DOCKERFILE"
dockerfilePath = "orbitalis-api/Dockerfile"
buildCommand = ""

[deploy]
healthcheckPath = "/api/v1/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
